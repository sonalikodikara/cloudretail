# API Gateway URL for development
# During local development, uncomment the first line
VITE_API_URL=http://localhost:9000

# For Docker Compose (when frontend container talks to api-gateway container)
# VITE_API_URL=http://api-gateway:9000
